import React from 'react';

export default function Accordion(props) {
  console.log(props);
  return (
    <div className='row direction-column'>
      {/* <button>{props.first.user.name}</button>
      <button>{props.second.user.name}</button>
      <button>{props.third.user.name}</button> */}
    </div>
  );
}
